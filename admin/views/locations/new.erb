<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:locations, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:locations, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :location, url(:locations, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'locations/form', :locals => { :f => f } %>
 <% end %>
</div>
