<ul class="nav nav-tabs">
  <li> <%= link_to tag_icon(:list, pat(:list)), url(:skills, :index) %></li>
  <li class="active"> <%= link_to tag_icon(:plus, pat(:new)), url(:skills, :new) %></li>
</ul>
<div class="tabs-content">
  <% form_for :skill, url(:skills, :create), :class => 'form-horizontal' do |f| %>
    <%= partial 'skills/form', :locals => { :f => f } %>
 <% end %>
</div>
